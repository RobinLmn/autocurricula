# Autocurricula

## Versioning

When preparing a PR to main, bump the version in `pyproject.toml`:
- **patch** (e.g. 0.2.0 → 0.2.1): bug fixes, minor tweaks
- **minor** (e.g. 0.2.1 → 0.3.0): new features, significant changes
- **major**: never — the author will do this manually
